[project]
name = "api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pi-sdk",
    "python-dotenv>=1.2.3",
    "unicorn>=2.1.4",
    "uvicorn>=0.52.4",
]

[tool.uv]
config-settings = { editable_mode = "compat" }

[tool.uv.sources]
pi-sdk = { path = "../../../pi-sdk", editable = true }
